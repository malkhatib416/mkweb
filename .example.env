## Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/mkweb

## Better Auth
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3003
# For client-side usage (optional, defaults to BETTER_AUTH_URL)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3003

## SMTP (for contact forms)
SMTP_SERVER_HOST=
SMTP_SERVER_USERNAME=
SMTP_SERVER_PASSWORD=

## reCAPTCHA
RECAPTCHA_SECRET_KEY=

## Optional: Google Tag Manager
GTM_ID=

## Optional: Replicate (for AI features)
REPLICATE_API_KEY=

## Supabase (client photo uploads + blog cover storage)
# Create a bucket named "client-photos" in Supabase Storage and allow public read if needed
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

## OpenAI (AI article generation + blog cover images via GPT Image API)
# Required for article generation and "Generate image" (gpt-image-1.5)
OPENAI_API_KEY=