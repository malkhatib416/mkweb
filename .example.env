## Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/mkweb

## Better Auth
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3003
# For client-side usage (optional, defaults to BETTER_AUTH_URL)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3003

## SMTP (for contact forms)
SMTP_SERVER_HOST=
SMTP_SERVER_USERNAME=
SMTP_SERVER_PASSWORD=

## reCAPTCHA
RECAPTCHA_SECRET_KEY=

## Optional: Google Tag Manager
GTM_ID=

## Optional: Replicate (for AI features)
REPLICATE_API_KEY=

## MinIO (S3-compatible storage for client photos + blog cover images)
# Match MINIO_ROOT_USER / MINIO_ROOT_PASSWORD from your MinIO container.
# In MinIO Console (http://localhost:9001), create bucket "uploads" and set Access Policy to "public" so image URLs work.
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=uploads
# Optional: public URL for images when MinIO is behind a proxy. Defaults to MINIO_ENDPOINT.
# MINIO_PUBLIC_URL=https://storage.example.com

## OpenAI (AI article generation + blog cover images via GPT Image API)
# Required for article generation and "Generate image" (dall-e-3)
OPENAI_API_KEY=